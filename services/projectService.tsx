// projectService.ts

export const joinProject = async () => {
  // В реальном приложении этот код будет отправлять запрос к серверу для присоединения к проекту
  // Например, через fetch или axios
  
  // Здесь используется setTimeout для имитации асинхронного запроса и получения данных
  return new Promise((resolve) => {
    setTimeout(() => {
      // Имитация успешного присоединения к проекту
      const project = {
        id: 123,
        name: 'Имя проекта',
        // Другие свойства проекта
      };
      resolve(project); // Возвращаем присоединенный проект
    }, 2000); // 2 секунды задержки для имитации асинхронного запроса
  });
};
